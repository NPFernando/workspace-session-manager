name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Install tmux
        run: sudo apt-get update && sudo apt-get install -y tmux
      - name: Install project
        run: uv sync --locked --extra dev
      - name: Ruff format
        run: uv run ruff format --check .
      - name: Ruff lint
        run: uv run ruff check .
      - name: Type check
        run: uv run mypy
      # Run the full coverage-gated unit/TUI suite once; other Python versions
      # still exercise the non-TUI unit suite without duplicating coverage.
      - name: Unit and TUI tests (coverage)
        if: matrix.python-version == '3.13'
        run: uv run pytest -m 'not integration and not layout_snapshot'
      - name: Unit tests (other Python versions)
        if: matrix.python-version != '3.13'
        run: uv run pytest -m 'not integration and not layout_snapshot' --ignore=tests/test_tui.py --no-cov
      - name: Isolated tmux integration test
        env:
          WS_RUN_TMUX_INTEGRATION: "1"
        run: uv run pytest -m integration -q --no-cov --snapshot-warn-unused
      - name: Secret and privacy scan
        run: scripts/secret-scan.sh

  layout-snapshot-monitor:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Install tmux
        run: sudo apt-get update && sudo apt-get install -y tmux
      - name: Install project
        run: uv sync --locked --extra dev
      - name: Layout snapshot monitor
        run: uv run pytest -m layout_snapshot -q --no-cov --junitxml=layout-snapshot-monitor.xml
      - name: Upload layout snapshot monitor report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: layout-snapshot-monitor-report
          path: layout-snapshot-monitor.xml
          if-no-files-found: error

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - run: uv build
      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          if-no-files-found: error
